generator client {
  provider = "prisma-client-js"
}



datasource db {
    provider = "postgresql"
    url = env("DATABASE_URL")
}

model tobacco_users {
  id       BigInt @id @default(autoincrement())
  email    String
  username String
  password String

  @@map("tobacco_users")
}

